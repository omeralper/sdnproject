{
  "name": "swagger-api-build",
  "description": "Gulp file for Swagger API build",
  "version": "0.4.0",
  "homepage": "http://www.argela.com/",
  "author": "Ekin Cam",
  "private": true,
  "scripts": {
    "preinstall": "npm config set package-lock false && npm cache clean --force && rm -rf node_modules && rm -f package-lock.json"
  },
  "dependencies": {
    "del": "^2.2.2",
    "gulp": "^3.9.1",
    "gulp-exec": "^2.1.3",
    "gulp-install": "^0.6.0"
  },
  "devDependencies": {
    "gulp-sonar": "^3.0.0",
    "nightwatch": "^0.9.9"
  }
}
